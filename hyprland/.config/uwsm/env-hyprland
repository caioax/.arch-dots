# ==============================================================================
# UWSM HYPRLAND SPECIFIC ENVIRONMENT
# ==============================================================================

# ------------------------------------------------------------------------------
# CURSORS / THEMES
# Define o tema e tamanho do cursor tanto para XWayland quanto Wayland nativo
# ------------------------------------------------------------------------------
export XCURSOR_THEME=Bibata-Modern-Classic
export XCURSOR_SIZE=24

export HYPRCURSOR_THEME=Bibata-Modern-Classic
export HYPRCURSOR_SIZE=24

# ------------------------------------------------------------------------------
# AQUAMARINE / RENDERING (Hyprland > 0.40)
# Configurações criticas de renderização e seleção de GPU
# ------------------------------------------------------------------------------
# Correção para alguns artefatos visuais (0 = desativado)
export AQ_FORCE_LINEAR_BLIT=0

# ------------------------------------------------------------------------------
# APPS & TOOLS CONFIGURATION
# Variáveis customizadas para scripts e ferramentas do usuário
# ------------------------------------------------------------------------------
# Diretório padrão para salvar capturas de tela do Hyprshot
export HYPRSHOT_DIR="$HOME/Pictures/Screenshots"

# Load local hyprland hardware exports (Aquamarine, etc)
if [ -f "$HOME/.config/uwsm/env.d/hyprland_hardware.sh" ]; then
    . "$HOME/.config/uwsm/env.d/hyprland_hardware.sh"
fi
